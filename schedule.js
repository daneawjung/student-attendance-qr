const form=document.getElementById('scheduleForm'),table=document.getElementById('table'),search=document.getElementById('search'),count=document.getElementById('count'),empty=document.getElementById('empty'),message=document.getElementById('message');let schedules=[];const days={0:'อาทิตย์',1:'จันทร์',2:'อังคาร',3:'พุธ',4:'พฤหัสบดี',5:'ศุกร์',6:'เสาร์'};function esc(v){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#039;','"':'&quot;'}[c]));}function msg(t,e=false){message.textContent=t;message.classList.remove('hidden');message.style.background=e?'#fee2e2':'#dcfce7';message.style.color=e?'#991b1b':'#166534';}async function load(){const {data,error}=await supabaseClient.from('attendance_weekly_schedules').select('*').order('day_of_week').order('start_time');if(error){msg('โหลดตารางไม่ได้: '+error.message,true);return;}schedules=data||[];render();}function render(){const q=search.value.trim().toLowerCase(),rows=schedules.filter(s=>[days[s.day_of_week],s.subject_code,s.subject_name,s.class_name,s.room].some(v=>String(v??'').toLowerCase().includes(q)));count.textContent=`${schedules.length} รายการ`;empty.classList.toggle('hidden',rows.length>0);table.innerHTML=rows.map(s=>`<tr><td>${days[s.day_of_week]}</td><td>${esc(s.start_time)}–${esc(s.end_time)}</td><td><strong>${esc(s.subject_code||'')}</strong><br>${esc(s.subject_name)}</td><td>${esc(s.class_name)}</td><td>${esc(s.room||'-')}</td><td>${s.active?'🟢 ใช้งาน':'⚪ ปิด'}</td><td class="actions"><button class="small-button" onclick="toggleSchedule('${encodeURIComponent(s.id)}',${!s.active})">${s.active?'ปิด':'เปิด'}</button></td></tr>`).join('');}form.addEventListener('submit',async e=>{e.preventDefault();const obj={day_of_week:Number(dayOfWeek.value),start_time:startTime.value,end_time:endTime.value,subject_code:subjectCode.value.trim()||null,subject_name:subjectName.value.trim(),class_name:className.value.trim(),room:room.value.trim()||null,active:true};if(obj.start_time>=obj.end_time){msg('เวลาเริ่มต้องน้อยกว่าเวลาสิ้นสุด',true);return;}const {error}=await supabaseClient.from('attendance_weekly_schedules').insert(obj);if(error){msg('บันทึกไม่ได้: '+error.message,true);return;}msg('บันทึกตารางเรียนสำเร็จ');form.reset();await load();});window.toggleSchedule=async function(id,active){const {error}=await supabaseClient.from('attendance_weekly_schedules').update({active}).eq('id',decodeURIComponent(id));if(error){msg('เปลี่ยนสถานะไม่ได้: '+error.message,true);return;}await load();};search.addEventListener('input',render);load();
